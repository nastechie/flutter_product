name: midterm_flutter_app
description: A Flutter app for managing products
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8  # Add this back
  http: ^1.1.0
  provider: ^6.1.1
  image_picker: ^1.0.4
  flutter_easyloading: ^3.0.5
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  # Remove or comment out assets if you don't have images
  # assets:
  #   - assets/images/